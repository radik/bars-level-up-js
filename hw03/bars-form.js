(function($){
    $.fn.validate = function(){
        // проверить, что this - это форма, а не какой-то другой dom-объект

        // провалидировать поля формы
        
        return this;
    }
}(jQuery));